import { DataVisualizationDirective } from './data-visualization.directive';

describe('DataVisualizationDirective', () => {
  it('should create an instance', () => {
    const directive = new DataVisualizationDirective();
    expect(directive).toBeTruthy();
  });
});
